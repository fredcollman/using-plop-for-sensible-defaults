import { render } from "@testing-library/react";
import {{ name }} from ".";

describe("{{ name }}", () => {
  it("renders a {{ name }}", () => {
    const { asFragment } = render(<{{ name }}>here is my content</{{ name }}>);
    expect(asFragment()).toMatchInlineSnapshot(`
      TODO
    `);
  });
});
