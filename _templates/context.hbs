"use client";
import { createContext, useContext, ReactNode } from "react";

interface {{ pascalCase name }} {
  greeting: string;
}

const {{ pascalCase name }}Context = createContext<{{ pascalCase name }} | null>(null);

const use{{ pascalCase name }} = () => useContext({{ pascalCase name }}Context);

interface Props {
  {{ lowerCase name }}: {{ pascalCase name }};
  children: ReactNode;
}

export const {{ pascalCase name }}Provider = ({ {{ lowerCase name }}, children }: Props) => {
  return (
    <{{ pascalCase name }}Context.Provider value={ {{ lowerCase name }} }>{children}</{{ pascalCase name }}Context.Provider>
  );
};

export default use{{ pascalCase name }};
